name: Build Native Android App (ripple.apk)

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Build Web App
        run: npm run build
        env:
          VITE_SUPABASE_URL: "https://mock-supabase.ripple.com"
          VITE_SUPABASE_ANON_KEY: "mock-anon-key"

      - name: Install Capacitor (stable v7)
        run: |
          npm install @capacitor/core@7.4.3 @capacitor/cli@7.4.3 @capacitor/android@7.4.3

      - name: Init Capacitor
        run: |
          if [ ! -f capacitor.config.ts ] && [ ! -f capacitor.config.json ]; then
            npx cap init Ripple com.ripple.app --web-dir=dist
          fi

      - name: Add Android Platform
        run: |
          if [ ! -d android ]; then
            npx cap add android
          fi

      - name: Sync Capacitor
        run: npx cap sync android

      # 🔥 THIS IS THE FIX THAT ACTUALLY SOLVES YOUR ERROR
      - name: Force Java 17 in Gradle (IMPORTANT)
        run: |
          find android -type f -name "*.gradle" -exec sed -i 's/VERSION_21/VERSION_17/g' {} \;
          find android -type f -name "*.gradle" -exec sed -i 's/sourceCompatibility *= *21/sourceCompatibility = 17/g' {} \;
          find android -type f -name "*.gradle" -exec sed -i 's/targetCompatibility *= *21/targetCompatibility = 17/g' {} \;

      - name: Verify Java
        run: |
          java -version
          javac -version

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Prepare APK
        run: |
          mkdir -p build-artifacts
          cp android/app/build/outputs/apk/debug/app-debug.apk build-artifacts/ripple.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ripple-apk
          path: build-artifacts/ripple.apk
